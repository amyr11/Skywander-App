import 'package:flutter/material.dart';

class VisaTrackerScreen extends StatelessWidget {
  const VisaTrackerScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}