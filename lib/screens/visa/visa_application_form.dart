import 'package:flutter/material.dart';

class VisaApplicationFormScreen extends StatelessWidget {
  const VisaApplicationFormScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}