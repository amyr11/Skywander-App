import 'package:flutter/material.dart';

class TypeOfVisaScreen extends StatelessWidget {
  const TypeOfVisaScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}